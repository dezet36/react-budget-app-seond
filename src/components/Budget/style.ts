import styled from "styled-components";

const StyledBadge = styled.div`
  display: grid;
  gap: 20px;
`;

const BadgeHeader = styled.div``;

const Title = styled.h2`
  font-family: "Inter";
  font-style: normal;
  font-weight: 700;
  font-size: 24px;
  line-height: 29px;
`;

export { BadgeHeader, Title, StyledBadge };
